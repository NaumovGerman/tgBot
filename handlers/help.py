from aiogram import Router
from aiogram.filters import Command, CommandStart
from aiogram.types import Message
from aiogram.enums import ParseMode

router = Router()

HELP_TEXT = """
🤖 <b>Фитнес-трекер бот</b>

Этот бот поможет вам отслеживать потребление воды, калорий и физическую активность.

<b>📋 Доступные команды:</b>

<b>/set_profile</b> — Настройка профиля
Первая команда, которую нужно выполнить. Бот запросит:
- Вес (кг)
- Рост (см)
- Возраст (лет)
- Минуты активности в день
- Город (для определения температуры)

На основе этих данных рассчитываются дневные нормы воды и калорий.

<b>/log_water [количество]</b> — Записать воду
Пример: /log_water 250
Добавляет указанное количество выпитой воды в миллилитрах.

<b>/log_food [название]</b> — Записать еду
Пример: /log_food apple
Бот найдет калорийность продукта, затем попросит указать вес в граммах.
Лучше работает с английскими названиями.

<b>/log_workout [тип] [минуты]</b> — Записать тренировку
Пример: /log_workout бег 30
Доступные типы: бег, ходьба, велосипед
Рассчитывает сожженные калории и увеличивает норму воды.

<b>/check_progress</b> — Показать прогресс
Отображает текущий прогресс по воде и калориям за день.

<b>/help</b> — Показать эту справку

<b>💡 Советы:</b>
- Начните с команды /set_profile
- Записывайте воду после каждого приема
- Логируйте еду сразу после приема пищи
- Не забывайте отмечать тренировки

<b>⚠️ Важно:</b>
Данные сбрасываются при перезапуске бота. В будущем будет добавлено сохранение в базу данных.
"""

@router.message(CommandStart())
async def start(message: Message):
    """Handler for /start command"""
    await message.answer(
        f"👋 Привет, {message.from_user.first_name}!\n\n"
        "Я фитнес-трекер бот. Помогу тебе следить за здоровьем!\n\n"
        "Используй /help чтобы увидеть все команды."
    )

@router.message(Command("help"))
async def help_command(message: Message):
    """Handler for /help command"""
    await message.answer(HELP_TEXT, parse_mode=ParseMode.HTML)